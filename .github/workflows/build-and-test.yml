name: Build and Test

on:
  push:
    branches: [ main ]  # Run workflow when code is pushed to the main branch

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Run tests
        run: |
          echo "Running tests..."
          npm test
